const BASE_URL = 'https://api.restful-api.dev';

const API_ENDPOINTS = {
  GET_DATA: `${BASE_URL}/objects/7`,
  GET_PROTECTED_DATA: `${BASE_URL}/protected-data`,
  POST_DATA: `${BASE_URL}/data`,
  // Add more endpoints as needed
};

export default API_ENDPOINTS;
